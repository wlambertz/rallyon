# Organizer Frontend (Placeholder)

This directory is intentionally empty. The Angular organizer portal that previously lived here has been removed while the frontend stack for RallyOn is reconsidered.

- No frontend framework, dependencies, or build tooling are currently installed here.
- No replacement stack has been selected yet.
- See `AGENTS.md` in this directory before doing any work here.

Once a frontend stack is chosen, this README and `AGENTS.md` should be rewritten to describe it.
