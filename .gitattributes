# Auto detect text files and perform LF normalization
* text=auto

# Ensure Windows batch files always use CRLF in Git
*.bat text eol=crlf

# Ensure Windows CMD scripts use CRLF
*.cmd text eol=crlf

# Ensure Unix shell scripts remain LF
*.sh text eol=lf

# Keep repo-local Codex skill files on LF for reliable discovery/parsing
skills/** text eol=lf

# Gradle wrapper scripts: keep Unix script LF
gradlew text eol=lf
service/tournamentmgmt/gradlew text eol=lf

# Maven wrapper scripts: keep Unix script LF
mvnw text eol=lf
service/tournamentmgmt/mvnw text eol=lf
